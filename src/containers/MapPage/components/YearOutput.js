import styled from 'styled-components';

const YearOutput = styled.div`
  text-align: center;
  padding-top: 30px;
  font-size: 20px;
  color: ${({ theme }) => theme.colors.gray};
`;

export default YearOutput;
